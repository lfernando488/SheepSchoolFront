import { CourseSubscription } from './course-subscription';

describe('CourseSubscription', () => {
  it('should create an instance', () => {
    expect(new CourseSubscription()).toBeTruthy();
  });
});
