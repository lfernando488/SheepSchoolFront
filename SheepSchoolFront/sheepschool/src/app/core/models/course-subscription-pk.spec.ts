import { CourseSubscriptionPk } from './course-subscription-pk';

describe('CourseSubscriptionPk', () => {
  it('should create an instance', () => {
    expect(new CourseSubscriptionPk()).toBeTruthy();
  });
});
